name: ${name}
description: ${description}
version: ${version}
author: ${author}
website: ${url}
main: org.geysermc.floodgate.SpigotPlugin
api-version: 1.13
folia-supported: true
commands:
  neteaseaccount:
    description: 梦想之城账号互通命令
    aliases:
      - neteasebind
      - naccount
      - nbind
permissions:
  floodgate.neteaseaccount.admin:
    description: 允许查看账号互通在线诊断
    default: op
  floodgate.neteasebind.admin:
    description: 兼容旧版账号互通在线诊断权限
    default: op
